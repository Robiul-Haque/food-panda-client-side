import React from 'react';

const Chef = (props) => {
    // console.log(props);

    // const { name } = props.chefs;

    return (
        <div>
            <h2>{name}</h2>
        </div>
    );
};

export default Chef;